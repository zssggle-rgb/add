# Test Plan
Generated by /plan-eng-review on 2026-05-08
Branch: bootstrap/add
Repo: add

## Affected Pages/Routes
- `file:///Users/sjs/add/index.html` — 加法器单页面应用

## Key Interactions to Verify
- 输入 `3` + `5` → 结果显示 `8`
- 输入 `-3` + `5` → 结果显示 `2`
- 输入 `1.5` + `2.5` → 结果显示 `4`
- 输入 `abc` → 显示错误状态 `×`
- 输入 `--5` → 显示错误状态（双重验证）
- 输入 `1..2` → 显示错误状态（双重验证）
- 空输入 + 数字 → 按 0 处理，显示正确结果

## Edge Cases
- 双空输入 → 结果 `0`
- Tab 导航焦点切换 → Focus 样式
- 移动端 480px viewport → 布局不溢出
- 超小屏幕 320px viewport → 布局不溢出
- 键盘全操作可访问性

## Critical Paths
- 正常加法：输入 → 验证 → parseFloat → calculate → showResult
- 错误恢复：非法输入 → showError → 修改为合法 → showResult
